---
import '@/styles/globals.css'
import MainHead from './MainHead.astro';

interface Props {
	title: string;
	description?: string;
	ogImage?: string;
}

const { title, description, ogImage }: Props = Astro.props;

---

<html lang="en">
	<MainHead {title} {description} ogImage={ogImage} />
	<body>
			<slot>Default text</slot>
	</body>
</html>
